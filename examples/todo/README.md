# [TodoMVC](https://todomvc.com/) in Zero
Run with:
```shell
npx shadow-cljs watch app
```
Open at <http://localhost:8000>.

## Tests
Build and run tests with:
```shell
npx shadow-cljs compile test && node target/node-tests.js
```
